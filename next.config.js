/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  distDir: 'dist',
  basePath: '',
  assetPrefix: 'https://majo-delatorre.github.io/', // tu URL de GitHub Pages
};

module.exports = nextConfig;
